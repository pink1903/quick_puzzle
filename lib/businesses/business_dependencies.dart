import 'package:get_it/get_it.dart';

class BusinessDependencies {
  static void init(GetIt injector) {
    // injector.registerLazySingleton<CaseTypeBusiness>(
    //       () => CaseTypeBusiness(injector()),
    // );
    // injector.registerLazySingleton<AuthBusiness>(
    //         () => AuthBusiness(injector()));
    // injector.registerLazySingleton<MasterDataBusiness>(
    //         () => MasterDataBusiness(injector()));
  }
}
