import 'package:get_it/get_it.dart';


class ServiceDependencies {
  static void init(GetIt injector) {
    // injector.registerLazySingleton(
    //       () => CaseTypeService(injector.call<RestUtils>(instanceName: 'AMS')),
    // );
    // injector.registerLazySingleton(
    //         () => MdmService(injector.call<RestUtils>(instanceName: 'MDM')));
    // injector.registerLazySingleton(
    //         () => MasterDataService(injector.call<RestUtils>(instanceName: 'AMS')));

  }
}
